using Mancala;
using Mancala.Code;

Table table = new Table(8);

bool isRunning = true;

while(isRunning)
{
	//render display
	Console.Clear();
	Mancala.UI.ConsoleApp.PrintTable(table);

	//get input

	//update game state

}
